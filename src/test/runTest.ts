/**
 * Test runner entry point.
 * This is used when running tests via `npm test`.
 * Tests can be run directly with mocha on the compiled output.
 */

// This file is a placeholder for future integration test support
// with @vscode/test-electron. For now, tests are run with mocha directly.
export {};
